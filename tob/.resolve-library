#!/bin/bash

source resolve-library || exit

assert_exitcode() {
    test "$1" = "$2" && return
    echo FAILED ASSERTION: exitcode "$1" = "$2"
    exit 1
    }

unset TOBLIB
resolve_library Object
assert_exitcode $? 1

TOBLIB=../lib
resolve_library Object
assert_exitcode $? 0

echo $0 DONE


